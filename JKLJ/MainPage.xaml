<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JKLJ.MainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Clicked="GoToListPage"
            Text="Zapisane wyniki"
            /> 
    </ContentPage.ToolbarItems>
    <StackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Text="Kobieta" VerticalOptions="Center" Grid.Row="0" Grid.Column="0"/>
            <Label Text="Mężczyzna" VerticalOptions="Center" Grid.Row="0" Grid.Column="1"/>

            <RadioButton x:Name="radio_gender_female"/>
            <RadioButton x:Name="radio_gender_male"/>
            
            
        </Grid>
    </StackLayout>

</ContentPage>
